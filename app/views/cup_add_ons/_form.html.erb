<%= form_for(cup_add_on) do |f| %>
  <% if cup_add_on.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cup_add_on.errors.count, "error") %> prohibited this cup_add_on from being saved:</h2>

      <ul>
      <% cup_add_on.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, "Nome" %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>


  <div class="row">
    <div class="col-lg-6">
      <div class="form-group">
        <%= f.label :image_url, "Imagem" %>
        <%= f.file_field :image_url, class: 'form-control' %>
      </div>
    </div>

    <div class="col-lg-6">
      <div class="form-group">
        <%= f.label :price, "Preço" %>
        <%= f.number_field :price, class: 'form-control', step: '0.01' %>
      </div>
    </div>
  </div>

  <div class="actions">
    <%= f.submit "Salvar", :class => "btn btn-success" %>
    <p>
  </div>
<% end %>
